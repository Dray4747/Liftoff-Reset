Данная программа создана для **сброса настроек Liftoff** по файлу конфигурации (config.xml).
> [!IMPORTANT]
> **Для корректной работы вам необходимо:**
> 1. Создать папку *Liftoff* на корне диска C.
> 2. В папке Liftoff должно быть находится: *Liftoff.exe*, *Liftoff_Data*. Если их нет, значит Liftoff установлен **НЕВЕРНО!**
> 3. Далее в папке *Liftoff_Data\Config* должен быть создан файл конфигурации ***config.xml***. Без него программа не будет заменять конфигурацию Liftoff.
> 4. В папку C:\Windows\System32\ скопировать файл [nhcolor](https://nhutils.ru/download/nhcolor.zip).

> [!TIP]
> Также рекомендуется **СКОПИРОВАТЬ** файл **System.xml** из скачанной версии Liftoff и назвать **frconfig.xml**. Этот файл нужен для ВОСТАНОВЛЕНИЯ конфига **System.xml**, в случае его утери.

> [!WARNING]
> Программа может вызвать реакцию антивируса, хотя сама по себе не является вирусом. Некоторые антивирусы склонны относить файл к классам повышенного риска или хакерским инструментам. **Происходит удаление файла и помещение его в карантин.**
>
> **В случае проблем с антивирусом добавьте файл в исключения**, либо **отключите детектирование PUA** (потенциально нежелательных приложений). Например, в касперском есть галочка "Обнаруживать легальные приложения, которые злоумышленники часто используют для нанесения вреда".
> 
> При аккуратной и правильной настройке исключений - рекомендуется настроить исключение, но если вы не до конца понимаете что делаете - рекомендуется отключить детект PUA.



